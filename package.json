{
  "name": "radio-paradise",
  "version": "1.2.0",
  "description": "Alexa Skill for querying playlist from Radio Paradise",
  "repository": {
    "type": "git",
    "url": "https://github.com/sgruendel/alexa-radio-paradise.git"
  },
  "main": "index.js",
  "scripts": {
    "invoke": "aws lambda invoke --function-name 'radioParadise' --payload fileb://../tests/events/alexa-start-session.json ../tests/out.txt && cat ../tests/out.txt",
    "test": "mocha",
    "deploy": "npm prune --production && npm run zip && npm run upload",
    "upload": "aws lambda update-function-code --function-name 'radioParadise' --zip-file fileb://../index.zip",
    "zip": "zip -r ../index.zip . -x 'package.json' 'test/*' '*.git*' '*~'"
  },
  "author": "Stefan Gründel",
  "license": "GPL-3.0",
  "dependencies": {
    "alexa-sdk": "^1.0.25",
    "cheerio": "^1.0.0-rc.2"
  },
  "devDependencies": {
    "aws-lambda-mock-context": "^3.1.1",
    "chai": "^4.1.2",
    "mocha": "^5.0.1"
  }
}
